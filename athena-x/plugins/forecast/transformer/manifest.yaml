id: transformer
name: "Transformer"
version: "1.0.0"
category: deep_learning
runtime: python-gpu
refresh_interval_seconds: 5
inputs: [features]
outputs: [direction, target_price, confidence, risk_estimate]
dependencies: []
enabled: true
description: "Transformer (NEVER browser)"
author: "ATHENA-X"
