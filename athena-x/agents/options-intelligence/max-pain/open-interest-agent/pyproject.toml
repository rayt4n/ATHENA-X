[project]
name = "athena_x_agent_options_intelligence_max_pain_open_interest_agent"
version = "0.1.0"
description = "Open Interest AI — options-intelligence agent"
requires-python = ">=3.11"
dependencies = [
    "athena-x-runtime-event-bus",
    "athena-x-runtime-logger",
    "athena-x-runtime-health-monitor",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/athena_x_agent_options_intelligence_max_pain_open_interest_agent"]
